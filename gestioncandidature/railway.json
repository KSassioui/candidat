{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.13"
    },
    "command": "pip install -r requirements.txt"
  },
  "start": {
    "command": "gunicorn gestioncandidature.wsgi --chdir gestioncandidature"
  },
  "deploy": {
    "command": "cd gestioncandidature && python manage.py migrate && python manage.py collectstatic --noinput"
}
}